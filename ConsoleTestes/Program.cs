using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Infraestrutura.NH;
using Infraestrutura.Repositorios.Entidades;
using StructureMap;

namespace ConsoleTestes
{
    class Program
    {
        static void Main(string[] args)
        {
            //NHibernateRegistry reg = ObjectFactory.GetInstance<NHibernateRegistry>();
            //NHibernateRegistry reg = new NHibernateRegistry();
            //ITemasRep _temasRep = ObjectFactory.GetInstance<ITemasRep>();
            //Temas tema = new Temas();
            //tema.Desc_tema = "Matemática";
            //_temasRep.Salvar(tema);
        }
    }
}
